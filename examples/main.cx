const x: int = 10;
let y: float = 1.5;

if (x <= y) {
    let x: int = 10;

    function add(x: int, y: int): int {
        return x + y;
    };
} else {
    return xs;
};

function ee(x: int, t: bool): int {
    return x;
};

if (x == t) {
    return x;
};